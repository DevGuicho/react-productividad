import React from 'react';
import '../assets/styles/components/ListProduct.css';
const ListProduct = ({ children }) => {
  return <div className='ListProduct'>{children}</div>;
};

export default ListProduct;
